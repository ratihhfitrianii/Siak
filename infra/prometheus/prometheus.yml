# Prometheus — scrape targets (docs/02 §10.1)
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'siak-backend'
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:3000']
