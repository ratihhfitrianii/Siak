import { Router } from 'express';

/**
 * Modul Dosen — Iterasi 3: pilih MK, ketersediaan jadwal, absensi, bimbingan, substitute (F-20~F-25).
 * Stub sementara.
 */
export function createDosenRouter(): Router {
  return Router();
}
