import { Router } from 'express';

/**
 * Modul Notifikasi — Iterasi 2: scheduler KRS + in-app + email plugin (AC-04d, F-25, K-09).
 * Stub sementara.
 */
export function createNotificationRouter(): Router {
  return Router();
}
