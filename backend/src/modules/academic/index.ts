import { Router } from 'express';

/**
 * Modul Akademik — T1.7/T1.8: struktur organisasi, kurikulum, kelas & jadwal, nilai dasar, transkrip (F-05, F-06, F-07b/c, F-10, F-16, F-22).
 * Stub sementara.
 */
export function createAcademicRouter(): Router {
  return Router();
}
