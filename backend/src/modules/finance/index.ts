import { Router } from 'express';

/**
 * Modul Keuangan — Iterasi 2: tagihan otomatis, pembayaran manual, payroll (F-08, F-08a~f, F-12, F-19, F-26).
 * Stub sementara.
 */
export function createFinanceRouter(): Router {
  return Router();
}
