import { Router } from 'express';

/**
 * Modul User & RBAC — T1.4: 5 tipe akun + atribut Wali + policy service (F-09, S-05, AC-10).
 * Stub sementara.
 */
export function createRbacRouter(): Router {
  return Router();
}
