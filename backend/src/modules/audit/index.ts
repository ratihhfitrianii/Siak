import { Router } from 'express';

/**
 * Modul Audit — T1.9: audit trail + atribusi "diinput oleh X" (F-13, S-06, S-07, AC-05).
 * Stub sementara.
 */
export function createAuditRouter(): Router {
  return Router();
}
