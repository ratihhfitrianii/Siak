import { Router } from 'express';

/**
 * Modul Auth — T1.3: login/logout/refresh JWT + bcrypt + rate limit (F-01, F-02, F-04, S-01, S-02, S-04).
 * Stub sementara: semua endpoint 501 SERVICE_UNAVAILABLE sampai T1.3 diimplementasikan.
 */
export function createAuthRouter(): Router {
  return Router();
}
